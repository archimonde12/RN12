import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  center: {
    backgroundColor: '#ddd',
    padding: 20,
  },
  heading: {
    fontSize: 20,
    fontWeight: 'bold',
  },
  error: {
    color: 'red',
  },
});

export default styles;
